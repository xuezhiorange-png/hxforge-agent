# Security Gate Spec (TASK-015 Section 4.1)
#
# Frozen contract: docs/tasks/TASK-015-ci-security-and-release-automation.md
# Frozen Contract Authority SHA: 39135e269b014e9c9310ac403a60591393d46b2d
# Section: 4.1 / 4.2 / 4.3 / 9.1 / 9.2 / 10 / 11.1

schema_version: 1
owner: xuezhiorange-png
updated_at: "2026-07-05T07:00:00Z"

# Stable repository-wide identifier (Section 4.3).
gate_id: security-gate

# failure_modes: closed enum drawn from docs/governance/failure_taxonomy.yaml
# (Section 4.2.4). Empty until security gates are added.
failure_modes: []

# content_hash: sha256 over canonical in-memory representation (Section 9.2).
content_hash: "f1a1b19d8318926b340f47e21a25da5f8bde82e4c8d9ad2f8b143626b3279f67"

# Security gates (Section 4.1: scan types, severity thresholds, retention).
# Empty skeleton; gates added via design + impl PRs (Section 4.2: append-only).
gates: []

# Restricted-content boundary (Section 10). Spec MUST NOT embed any of
# the categories listed in Section 10; CI lint raises
# RestrictedContentViolation as BLOCKER if found.
restricted_source_categories:
  - standard_body
  - vendor_catalog_body
  - paid_price_list
  - restricted_property_table
  - scanned_page
  - formula_image
  - copied_standard_table

# Required status check names (Section 7 / 9 cross-cutting).
required_status_checks:
  - task-015/security-gate/blockers
  - task-015/security-gate/restricted-content