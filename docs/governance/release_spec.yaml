# Release Spec (TASK-015 Section 4.1)
#
# Frozen contract: docs/tasks/TASK-015-ci-security-and-release-automation.md
# Frozen Contract Authority SHA: 39135e269b014e9c9310ac403a60591393d46b2d
# Section: 4.1 / 4.2 / 4.3 / 9.1 / 9.2 / 11.1 / 11.3

schema_version: 1
owner: xuezhiorange-png
updated_at: "2026-07-05T07:00:00Z"

# Stable repository-wide identifier (Section 4.3).
# Examples: nightly, prerelease, stable.
release_channel: stable

# failure_modes: closed enum drawn from docs/governance/failure_taxonomy.yaml
# (Section 4.2.4). Empty until release channels are added.
failure_modes: []

# content_hash: sha256 over canonical in-memory representation (Section 9.2).
content_hash: "58d2e53f7865010175aa7cdfb8c02d9d188189e5749698a8c124fc59592faa32"

# Release channels (Section 4.1: tag pattern, artifact pattern,
# attestation requirement). Empty skeleton; channels added via design + impl
# PRs (Section 4.2: append-only).
channels: []

# Release-gate requirements (Section 11.3 item 14). A tag matching the tag
# pattern triggers a release-gate check; the gate requires content_hash
# stability and a frozen-contract reference declaration.
release_gate:
  require_content_hash_stability: true
  require_frozen_contract_reference: true
  # Frozen contracts that must be referenced by the gate.
  frozen_contract_references:
    - task_011_frozen_contract
    - task_012_frozen_contract
    - task_013_frozen_contract
    - task_014_frozen_contract
    - task_015_frozen_contract

# Required status check names (Section 7 / 9 cross-cutting).
required_status_checks:
  - task-015/release/blockers
  - task-015/release/gate